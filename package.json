{
  "name": "capacitor-background-fetch",
  "version": "0.5.2",
  "description": "Utilizing the iOS background fetch mechanism to fetch new data while the app is in background",
  "main": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf ./dist",
    "watch": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "kaunstdadenga",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "1.0.0"
  },
  "devDependencies": {
    "typescript": "^3.2.4",
    "@capacitor/ios": "1.0.0"
  },
  "files": [
    "dist/",
    "ios/",
    "CapacitorBackgroundFetch.podspec"
  ],
  "keywords": [
    "capacitor",
    "plugin",
    "native",
    "ios",
    "background fetch"
  ],
  "capacitor": {
    "ios": {
      "src": "ios"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lbslogics/capacitor-background-fetch.git"
  },
  "bugs": {
    "url": "https://github.com/lbslogics/capacitor-background-fetch.git/issues"
  }
}
